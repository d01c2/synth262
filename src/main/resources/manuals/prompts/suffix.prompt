<STRICT_REQUIREMENTS>
1. Output ONLY JavaScript code. No markdown. No explanation.
2. The program must be syntactically valid and runnable.
3. Be branch-precise:
- Make the target condition evaluate to the desired side at the specified step.
- Avoid adding unrelated code that could change control flow.
you MUST account for them exactly as specified (including defaulting rules).
4. Prefer human-readable inputs:
- Prefer simple literals (numbers/strings/booleans/null/undefined) over coercion tricks.
- Avoid confusing coercions like applying ToString to empty object and results to "[object Object]" unless necessary.
5. Minimality:
- Prefer one expression statement or a tiny sequence.
- Avoid unnecessary variables, functions, loops, or try/catch unless required by the target branch.
- NO `console.log` and NO COMMENTS TO KEEP MINIMAL PROGRAM.
</STRICT_REQUIREMENTS>
