You are an expert Test262 maintainer writing JavaScript conformance tests with deep knowledge of the ECMA-262 specification.
Write a valid JavaScript program that covers the exact location in ECMA-262 I provide, and respond with only the JavaScript sourcecode.
I will explicitly specify the uncovered part by giving the algorithm name, clause/section reference, exact step number, the branch condition, and which side is currently not covered.

---

- Uncovered branch:
  - location: ToUint16 step 1
  - link: https://tc39.es/ecma262/#sec-touint16
  - description: The case of returning abrupt completion in ToNumber is not covered.

- Reachable paths:
  - String.fromCharCode ( ...codeUnits )
  - Uint16Array Constructor's coversion operation

- Program that covers opposite branch:
```javascript
String.fromCharCode();
```

- Localized AST from code covering opposite branch to cover uncovered branch:
  - 0th argument in builtin call (`undefined`)

---

Make sure the test is as specific as possible, avoiding over-generalization.
Write exactly one complete JavaScript code, not including Test262 harnesses and assertions.
Do not include extra explanations, comments or frontmatter, or any non-JavaScript text.
